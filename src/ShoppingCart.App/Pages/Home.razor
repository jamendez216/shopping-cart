@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to groceries page on load
        Navigation.NavigateTo("/groceries");
    }
}
